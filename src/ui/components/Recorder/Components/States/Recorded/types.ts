export interface StateProps {
  handleOnRecorderClose;
  audioUrl;
  openSlider;
  slider;
  sampleRate;
  onUpdateSampleRate;
  onDefaultSampleRateClick;
  onSave;
  closeSlider;
  onStart;
  onSampleRateSave;
  deviceLabel;
}
